class Solution {
    public int solution(int n) {
        int answer = 1;
        int idx = n;
        while(answer<=idx){
            if(answer%3==0) idx++;
            if(answer%3!=0&&String.valueOf(answer).contains("3")) idx++;
            answer++;
        }
        return answer;
    }
}
